<% flash.each do |type, message| %>
    <div class="alert alert-<%= bootstrap_class_for(type) %> alert-dismissable no-margin">
        <button class="close" data-dismiss="alert aria-hidden="true">&times;</button>
        <p><%= message %></p>
    </div>
<% end %>